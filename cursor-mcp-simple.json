{
  "mcpServers": {
    "docker-mcp-gateway": {
      "command": "docker",
      "args": [
        "exec",
        "-i",
        "mcp-gateway",
        "/docker-mcp",
        "gateway",
        "--stdio"
      ]
    }
  }
}
